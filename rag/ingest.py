"""Chunk + embed corpus markdown files into ChromaDB. Placeholder."""


def ingest_corpus(corpus_dir: str = "corpus") -> int:
    return 0


if __name__ == "__main__":
    count = ingest_corpus()
    print(f"Ingested {count} chunks")
